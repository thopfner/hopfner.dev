# Coding Agent Prompt

You are executing a premium polish and consistency batch, not a new feature sprint.

The audit conclusion is:

1. the visual editor now looks strong live, but the canvas chrome still carries too much small-label noise
2. the structure rail and page settings panel still read more like internal control surfaces than elite product surfaces
3. schema-backed composed sections still contradict the visual-editor story because the canvas path is blocked while the inspector path exists

Execution rules:

1. execute phases in order
2. stop if a phase gate fails
3. preserve the current architecture and improve it surgically
4. reduce noise while increasing clarity
5. if an assumption in the brief breaks, stop and report the exact file and contract mismatch

Anti-drift rules:

1. do not add more status chrome than you remove
2. do not move important actions further away from the user
3. do not solve clarity problems by exposing more raw metadata
4. do not leave schema-backed composed sections visually blocked on canvas
5. do not use source-string tests as the main proof of success

Required quality bar:

1. the editor must feel calmer, clearer, and more premium at first glance
2. the structure rail must communicate useful information faster
3. page settings must feel like a polished workspace panel, not a fallback form
4. composed sections must stop feeling like second-class citizens when the current schema path already supports them

Required reporting:

1. state the exact canvas chrome elements removed or consolidated
2. state the exact structure-rail information hierarchy after the change
3. state the exact page-panel hierarchy changes
4. state the exact composed-section canvas block that was removed or narrowed
5. list the new tests by filename and scenario
