# Enhancements v15

Use this batch only after v14 is complete and live.

Primary file:

- `semantic-alignment-for-promoted-sections.md`

Purpose:

- give the newly promoted homepage sections the same semantic formatting system and elite UI treatment as the rest of the site
- remove bespoke visual drift
- align trust/proof/story/process sections with the shared design system

Do not execute this batch against the old generic composed versions.
